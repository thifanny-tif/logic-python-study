# 3 notas e verifique se ele esta aprovado ou reprovado
nota1 = float(input('digite a PRIMEIRA Nota.:'))
nota2 = float(input('digite a SEGUNDA Nota.:'))
nota3 = float(input('digite a TERCEIRA Nota.:'))
media = (nota1 + nota2 + nota3)/3

if media >= 7:
    print("Aprovado")
else:
    print("Reprovado")
